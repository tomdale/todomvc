(function( win ) {
	'use strict';

	// Create an application object.  The application serves as the namespace in
	// which all of your other classes will live (to avoid polluting the global
	// namespace).

	win.Todos = Ember.Application.create();

})( window );
